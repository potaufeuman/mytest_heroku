<% provide(:title, @user.name) %>
<div class = "row">
  <%= render "side_menu", user: @user %>
  <div class="col-md-7 white_block">
    <% if @user.tests.any? %>
      <h3>Tests you created (<%= @user.tests.count %>)</h3>
      <ol class="microposts">
        <%= render @tests, user: @user  %>
      </ol>
      <%= will_paginate @tests %>
    <% end %>
  </div>
</div>
