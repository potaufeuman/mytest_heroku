en:                              ## 言語名
  activerecord:
    models:
      user: user
      admin: admin
    attributes:
      test:
        id: ID
        分野: subject
        問題のタイトル: title
      admin:
        id: ID